image: edbizarro/gitlab-ci-pipeline-php:7.3

stages:
  - Build and Publish

build_and_publish:
  stage: Build and Publish
  script:
    - npm install
    - npm run prod
    - node deploy.js
  only:
    refs:
      - master
    changes:
      - src/**/*.{js, css}
